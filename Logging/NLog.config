<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.nlog-project.org/schemas/NLog.xsd NLog.xsd"
      autoReload="true"
      throwExceptions="false">
    <variable name="logDir" value="${basedir}/logs/" />
    <targets>
        <target xsi:type="File" name="fileUI"
                fileName="${logDir}/UI_${shortdate}.log"
                layout="${longdate} ${uppercase:${level}} ${message}"
                archiveFileName="${logDir}/Archive/UI_${shortdate}.log"
                archiveEvery="Day"
                maxArchiveFiles="30" />
        <target xsi:type="File" name="fileBusiness"
                fileName="${logDir}/Business_${shortdate}.log"
                layout="${longdate} ${uppercase:${level}} ${message}"
                archiveFileName="${logDir}/Archive/Business_${shortdate}.log"
                archiveEvery="Day"
                maxArchiveFiles="30" />
        <target xsi:type="ColoredConsole" name="console"
               layout="${longdate} ${uppercase:${level}} ${message}" />
    </targets>
    <rules>
        <logger name="*"        minlevel="Trace" writeTo="console" />
        <logger name="UI"       minlevel="Trace" writeTo="fileUI" />
        <logger name="Business" minlevel="Trace" writeTo="fileBusiness" />
    </rules>
</nlog>